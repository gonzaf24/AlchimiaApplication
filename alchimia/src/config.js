

export const CONFIG_ACTIVIDAD = {
  bucketName: bucketName,
  dirName: "actividad",
  region: region,
  accessKeyId: accessKeyId,
  secretAccessKey: secretAccessKey,
};

export const CONFIG_ALBUM = {
  bucketName: bucketName,
  dirName: "album",
  region: region,
  accessKeyId: accessKeyId,
  secretAccessKey: secretAccessKey,
};

export const CONFIG_PODCAST_PORTADA = {
  bucketName: bucketName,
  dirName: "podcastPortada",
  region: region,
  accessKeyId: accessKeyId,
  secretAccessKey: secretAccessKey,
};

export const CONFIG_PODCAST_AUDIO = {
  bucketName: bucketName,
  dirName: "podcastAudio",
  region: region,
  accessKeyId: accessKeyId,
  secretAccessKey: secretAccessKey,
};

export const CONFIG_AVATAR = {
  bucketName: bucketName,
  dirName: "avatar",
  region: region,
  accessKeyId: accessKeyId,
  secretAccessKey: secretAccessKey,
};
